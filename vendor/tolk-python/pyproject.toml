[tool.poetry]
name = "tolk"
version = "0.1.0"
description = "Screen reader abstraction library"
authors = ["Davy Kager <mail@davykager.nl>"]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
